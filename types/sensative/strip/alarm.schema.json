{
  "$id": "https://akenza.io/sensative/strip/alarm.schema.json",
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Alarm",
  "type": "object",
  "properties": {
    "highAlarm": {
      "title": "High alarm",
      "description": "Equals true if the threshold is exceeded",
      "type": "boolean"
    },
    "lowAlarm": {
      "title": "Low alarm",
      "description": "Equals true if the threshold is fallen below",
      "type": "boolean"
    },
    "doorAlarm": {
      "title": "Door alarm",
      "description": "Equals true if theres a door alarm",
      "type": "boolean"
    },
    "tamperAlarm": {
      "title": "Tamper alarm",
      "description": "Equals true if the strip has been tampered with",
      "type": "boolean"
    },
    "floodAlarm": {
      "title": "Flood alarm",
      "description": "Equals true if a flood is detected",
      "type": "boolean"
    },
    "foilAlarm": {
      "title": "Foil alarm",
      "description": "Equals true if a foil is detected",
      "type": "boolean"
    },
    "userSwitch1Alarm": {
      "title": "User switch alarm",
      "description": "Equals true if a user switch is detected",
      "type": "boolean"
    },
    "closeProximityAlarm": {
      "title": "Close proximity Alarm",
      "description": "Equals true if an object in close proximity is detected",
      "type": "boolean"
    },
    "disinfectAlarm": {
      "title": "Disinfect alarm",
      "description": "Status of the disinfection",
      "type": "string",
      "enum": [
        "dirty",
        "occupied",
        "cleaning",
        "clean"
      ]
    }
  }
}