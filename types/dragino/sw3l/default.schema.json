{
  "$id": "https://akenza.io/dragino/sw3l/default.schema.json",
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "processingType": "uplink_decoder",
  "topic": "default",
  "title": "Default",
  "properties": {
    "calculateFlag": {
      "title": "Calculate flag",
      "description": "Transforms the pulse into liter if set. Needs a command to be active.",
      "type": "integer",
      "hideFromKpis": true
    },
    "alarm": {
      "title": "Alarm",
      "description": "Equals true if the alarm is triggered.",
      "type": "boolean"
    },
    "waterFlow": {
      "title": "Water flow",
      "unit": "l",
      "description": "Calculated water flow in liters with the transformation.",
      "type": "number"
    },
    "lastPulse": {
      "title": "Last pulse",
      "description": "Pulses detected between the last event and now.",
      "type": "integer"
    },
    "totalPulse": {
      "title": "Total pulse",
      "description": "Total of pulses detected since reset.",
      "type": "integer"
    }
  }
}