{
  "$id": "https://akenza.io/haltian/thingseeEnvironment/move_count.schema.json",
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Move count",
  "type": "object",
  "properties": {
    "moveCount": {
      "title": "Movement count",
      "description": "Movement count is an integer where min = 0 and max = none. It contains the count of detections since the last reported movement report.",
      "type": "integer",
      "minimum": 0,
      "maximum": 1
    }
  }
}
