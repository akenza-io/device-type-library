{
  "$id": "https://akenza.io/enless/600-032/alarm.schema.json",
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "processingType": "uplink_decoder",
  "topic": "alarm",
  "title": "Alarm Status",
  "properties": {
    "temperatureLow": {
      "type": "boolean",
      "title": "Temperature Low Alarm",
      "description": "Indicates if the temperature is below the threshold"
    },
    "temperatureHigh": {
      "type": "boolean",
      "title": "Temperature High Alarm",
      "description": "Indicates if the temperature is above the threshold"
    }
  },
  "required": [
    "temperatureLow",
    "temperatureHigh"
  ]
}