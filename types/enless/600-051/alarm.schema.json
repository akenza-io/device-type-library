{
  "$id": "https://akenza.io/enless/600-051/alarm.schema.json",
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "processingType": "uplink_decoder",
  "topic": "alarm",
  "title": "Alarm Status",
  "properties": {
    "humidityLow": {
      "type": "boolean",
      "title": "Humidity Low Alarm",
      "description": "Indicates if the humidity is below the threshold"
    },
    "humidityHigh": {
      "type": "boolean",
      "title": "Humidity High Alarm",
      "description": "Indicates if the humidity is above the threshold"
    },
    "temperatureLow": {
      "type": "boolean",
      "title": "Temperature Low Alarm",
      "description": "Indicates if the temperature is below the threshold"
    },
    "temperatureHigh": {
      "type": "boolean",
      "title": "Temperature High Alarm",
      "description": "Indicates if the temperature is above the threshold"
    },
    "motionGuard": {
      "type": "boolean",
      "title": "Motion Guard Alarm",
      "description": "Indicates if a motion guard alarm was triggered"
    }
  },
  "required": [
    "humidityLow",
    "humidityHigh",
    "temperatureLow",
    "temperatureHigh",
    "motionGuard"
  ]
}