## Notes

Some sensor settings are only described in the technical documentation. These are still included in the code for backwards compatibility and completions sake.
The setting ids (type) are reserved and not reused, so it should not lead to issues with future versions.
